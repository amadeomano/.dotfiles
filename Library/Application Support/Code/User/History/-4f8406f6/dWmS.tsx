import { InfoItem, InfoPicker } from './types';
import { useLegalEntities } from '../../../hooks/useLegalEntities';

export const LegalEntitiesPicker = () => {
  const { legalEntities } = useLegalEntities();

  return;
};
