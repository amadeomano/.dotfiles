/* DO NOT DELETE - This file is automatically generated and should not be modified */

import request from '@personio-web/request';
import type {
  PollFiling,
  PollFilingDataResponse,
  PollFilingPathParams,
  PollFilingAPI,
} from '@personio-web/payroll-data-payroll-me-types';
import { pollFilingAPI } from '../common';

const replacePathParams = (
  url: PollFilingAPI['API_PATH'],
  params: PollFilingPathParams,
) => url.replace(/(:(.*?))(?![^/])/gs, (_1, _2, key) => params[key]);

export const pollFiling: PollFiling = ({ data, pathParams, headers }) =>
  request(replacePathParams(pollFilingAPI.API_PATH, pathParams), {
    method: pollFilingAPI.METHOD,
    data,
    headers,
  }).then(({ data }: PollFilingDataResponse) => data);
