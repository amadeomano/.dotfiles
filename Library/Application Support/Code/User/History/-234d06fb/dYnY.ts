/* DO NOT DELETE - This file is automatically generated and should not be modified */

import request from '@personio-web/request';
import type {
  ViewFiling,
  ViewFilingDataResponse,
  ViewFilingPathParams,
  ViewFilingAPI,
} from '@personio-web/payroll-data-payroll-me-types';
import { viewFilingAPI } from '../common';

const replacePathParams = (
  url: ViewFilingAPI['API_PATH'],
  params: ViewFilingPathParams,
) => url.replace(/(:(.*?))(?![^/])/gs, (_1, _2, key) => params[key]);

export const viewFiling: ViewFiling = ({ pathParams, headers }) =>
  request(replacePathParams(viewFilingAPI.API_PATH, pathParams), {
    method: viewFilingAPI.METHOD,
    headers,
  }).then(({ data }: ViewFilingDataResponse) => data);
