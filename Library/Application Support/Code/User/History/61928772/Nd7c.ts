/* DO NOT DELETE - This file is automatically generated and should not be modified */

import request from '@personio-web/request';
import type {
  SubmitFiling,
  SubmitFilingDataResponse,
  SubmitFilingPathParams,
  SubmitFilingAPI,
} from '@personio-web/payroll-data-payroll-me-types';
import { submitFilingAPI } from '../common';

const replacePathParams = (
  url: SubmitFilingAPI['API_PATH'],
  params: SubmitFilingPathParams,
) => url.replace(/(:(.*?))(?![^/])/gs, (_1, _2, key) => params[key]);

export const submitFiling: SubmitFiling = ({ data, pathParams, headers }) =>
  request(replacePathParams(submitFilingAPI.API_PATH, pathParams), {
    method: submitFilingAPI.METHOD,
    data,
    headers,
  }).then(({ data }: SubmitFilingDataResponse) => data);
